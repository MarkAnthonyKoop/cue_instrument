[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cue_instrument"
version = "0.1.0"
description = "Pool cue as musical instrument via accel/vibration/video/audio transducers."
requires-python = ">=3.10"
dependencies = []
